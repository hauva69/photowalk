#!/bin/sh

# Make tifs.
ufraw-batch --wb=camera --gamma=0.45 --linearity=0.10 --exposure=0.0 --saturation=1.0 --out-type=tiff --overwrite "$@"

# Align
# align_image_stack -a AIS_ IMG_????.tif

enfuse -o enfuse.tif dsc_????.tif

